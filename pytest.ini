[pytest]
testpaths = tests
markers =
    integration: requires MySQL and/or Kafka services (skipped automatically when unavailable)
    fullstack: requires the complete docker-compose stack (all service containers running)
addopts = -ra --strict-markers
